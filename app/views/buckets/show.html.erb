<p>
  <strong>Title:</strong>
  <%= @bucket.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @bucket.text %>
</p>

<%= link_to 'Back', buckets_path %>
<%= link_to 'Edit', edit_bucket_path(@bucket) %>
<%= link_to 'Destroy', bucket_path(@bucket),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>
